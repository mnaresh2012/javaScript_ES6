let x = BigInt(45); // one way to declare bigInt
let y = 50n; // second way of declaring bigInt

// we can'nt do arithmetic operation between number and bigInt
// bigInt can not store floating value
// it is used to store bigger integer value

// document.write(x * y);

typeof x // bigint