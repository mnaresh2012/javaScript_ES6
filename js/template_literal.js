// In ES6 two way we can define a string

let userName = 'Naresh'; // old way

let newUserName = `Naresh Chandra Mahanta`;
console.log(newUserName);

const greet = (msg) => {
    console.log(`Hi! ${msg} `);
}

greet('Naresh'); // output: Hi! Naresh